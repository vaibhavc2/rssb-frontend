import { sanitizedConfig } from "./config";

export const {
  APPWRITE_URL,
  APPWRITE_PROJECT_ID,
  APPWRITE_BUCKET_ID,
  APPWRITE_COLLECTION_ID,
  APPWRITE_DATABASE_ID,
  GITHUB_PROFILE_API,
} = sanitizedConfig;
